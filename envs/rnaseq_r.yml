# Environment: rnaseq_r
# Used for: Stages 8-9 (DESeq2, clusterProfiler, visualization)
# To recreate:
#   conda create -n rnaseq_r -c conda-forge r-base -y
#   conda activate rnaseq_r
#   R  (then run scripts/install_r_packages.R)

name: rnaseq_r
channels:
  - conda-forge
dependencies:
  - r-base